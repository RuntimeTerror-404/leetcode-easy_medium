#include <bits/stdc++.h>
using namespace std;

class solution
{
    bool wb(string s, int pos)
    {
        if (pos == s.size())
            return true;

        for (int i = pos; i < s.size(); ++i)
        {
            // if(setCOntains(s.substr(pos, i-pos+1), wb(s, pos+1))) true
        }
        return false;
    }
};

int main()
{
}